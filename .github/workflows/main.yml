name: any git 
on:
  push:
    branches: ['main']
jobs:
  run-app:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - run: npm i 
      - run: pm2 start npm --name frontend -- run dev -- --host 0.0.0.0 --port 5173
